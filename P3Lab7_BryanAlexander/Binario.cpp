#include <iostream>
#include <string>
#include "Binario.hpp"
using namespace std;
    Binario::Binario() {}
    Binario::Binario(string numero, char car)  {
        this->num= numero;
    }
    void Binario::convertir(string num) {
    }