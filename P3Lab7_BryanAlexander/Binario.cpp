#include <iostream>
#include "Binario.hpp"
using namespace std;
public:
    Binario::Binario() {}
    Binario::Binario(string numero) {
        this->num=numerto;
    }
    void Binario::convertir(string num) {
        return 0;
    }